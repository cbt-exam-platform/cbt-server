# Server
API + Authentication + Server web application for the CBT framework

## Requirements
- Runtime: Node.js (LTS)
- Framework: Express.js (REST API)
- Authentication: JWT-based authentication
- Database: PostgreSQL (via Prisma or Sequelize ORM)
- API Gateway: Centralized request routing and rate limiting
- Reverse Proxy: Nginx (SSL termination and request forwarding)
